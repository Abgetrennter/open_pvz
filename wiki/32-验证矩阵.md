# 验证矩阵

> 这篇文档把 [验证清单](15-验证清单.md) 从“说明当前有哪些验证入口”进一步推进到“工程上如何持续维护验证覆盖”的矩阵文档。

---

## 文档定位

这篇文档主要回答：

- 当前主干能力分别由哪些场景和命令覆盖
- 哪些验证已经通过，哪些仍缺口明显
- 每个验证项应观察什么结果
- 后续新增能力时，如何把它挂进统一矩阵

这篇文档不负责：

- 重新解释验证脚本的细节
- 代替路线图阶段判断

---

## 状态说明

矩阵中的状态建议统一使用：

- `已通过`
- `部分覆盖`
- `待补充`
- `阻塞`

如果某项验证只在单场景或人工观察下成立，应标为 `部分覆盖`，而不是直接写成 `已通过`。

---

## 当前固定入口

### 1. 默认主场景

- [`scenes/validation/minimal_battle_validation.tscn`](../scenes/validation/minimal_battle_validation.tscn)

### 2. 主干验证资源

- [`scenes/validation/minimal_battle_validation.tres`](../scenes/validation/minimal_battle_validation.tres)

### 3. 长距离抛物线专项验证

- [`scenes/validation/parabola_long_range_validation.tres`](../scenes/validation/parabola_long_range_validation.tres)

### 4. 自动化脚本

- [`tools/run_validation.ps1`](../tools/run_validation.ps1)

Godot console 路径和本地命令约定见：

- [`agent.md`](../agent.md)

---

## 当前主干验证矩阵

| 能力域 | 验证项 | 场景 / 资源 | 入口 | 关键观察点 | 当前状态 | 备注 |
|------|------|------|------|------|------|------|
| 启动与资源加载 | 默认主场景能加载 | `minimal_battle_validation.tscn` | headless 启动 | 无脚本解析错误、无场景加载错误 | 已通过 | 作为所有改动后的基础检查 |
| 事件总线 | 结构化事件链可广播 | `minimal_battle_validation.tres` | 默认主场景或指定场景 | `game.tick` 与后续事件链出现 | 已通过 | 当前可观察 `event_id / chain_id / depth` |
| 触发器执行 | 周期触发能驱动效果 | `minimal_battle_validation.tres` | 默认主场景 | `game.tick -> trigger -> effect` | 已通过 | 当前由最小战斗场景覆盖 |
| 基础效果 | `spawn_projectile / damage / explode` 可执行 | `minimal_battle_validation.tres` | 默认主场景 | 事件链与效果日志一致 | 已通过 | 仍需后续补更多效果类型 |
| 直线投射物 | 线性投射体可生成和命中 | `minimal_battle_validation.tres` | 默认主场景 | `projectile.spawned` 与 `projectile.hit` | 已通过 | 当前主干已覆盖 |
| 追踪投射物 | 追踪投射体可生成和命中 | `minimal_battle_validation.tres` | 默认主场景 | `move_mode = track` 的生成与命中 | 已通过 | 当前主干已覆盖 |
| 抛物线投射物 | 抛射体能在终点命中 | `minimal_battle_validation.tres` | 默认主场景 | `move_mode = parabola` 且终点命中 | 已通过 | 已转为地面平面 + 高度规则 |
| 长距离抛物线 | 远距离抛射体不提前消失且能命中 | `parabola_long_range_validation.tres` | `.\tools\run_validation.ps1` | `projectile.spawned -> projectile.hit -> entity.damaged` | 已通过 | 当前有专项自动化入口 |
| 死亡触发链 | 死亡后续效果可回写 | `minimal_battle_validation.tres` | 默认主场景 | `entity.died` 与爆炸伤害链 | 已通过 | 覆盖最小连锁 |
| 高度规则 | `HeightBand` 对命中真正生效 | 暂缺专项场景 | 暂缺 | 不同高度目标的命中差异 | 待补充 | 这是当前最明显缺口之一 |
| 多 lane 交叉 | 轨迹不会被错误 lane 截获 | 当前由最小场景间接覆盖 | 默认主场景 | 命中目标与预期 lane 一致 | 部分覆盖 | 需要独立专项场景 |
| 高速扫掠命中 | `swept_segment` 对高速弹稳定生效 | 暂缺专项场景 | 暂缺 | 快速轨迹不会穿透漏判 | 待补充 | 面向后续斜线/曲线轨迹 |
| 模板实例化 | 模板驱动实体生成 | 暂缺 | 暂缺 | `EntityTemplate` 真正进入生成主链 | 待补充 | 属于下一阶段关键出口 |
| 批量回归 | 多场景可一次性执行 | 暂缺 | 暂缺 | 统一汇总 pass/fail | 待补充 | 需要 `run_all_validations.ps1` |

---

## 当前推荐命令

### 1. 基础 headless 启动

```powershell
& '<GodotConsole>' --headless --path '<project_root>' --quit-after 3
```

### 2. 跑当前默认自动化验证

```powershell
& '.\tools\run_validation.ps1'
```

### 3. 指定验证资源

```powershell
& '.\tools\run_validation.ps1' -Scenario 'res://scenes/validation/minimal_battle_validation.tres'
```

### 4. 打开运行时快照日志

```powershell
& '<GodotConsole>' --headless --path '<project_root>' --quit-after 240 -- --runtime-snapshot-log --runtime-snapshot-interval=30
```

---

## 新增验证项时的维护规则

以后每增加一个新的主干能力，建议至少回答下面 6 个问题。

1. 它属于哪个能力域。
2. 它对应哪个最小验证场景。
3. 它是否有自动化入口。
4. 它的关键日志看什么。
5. 当前状态是 `已通过 / 部分覆盖 / 待补充 / 阻塞` 哪一种。
6. 如果还没通过，下一步要补什么。

如果一项能力已经进入主干，但在这张矩阵里没有位置，说明验证体系还没有真正接住它。

---

## 当前最应该补的矩阵空洞

按优先级看，当前最值得补的是：

1. 高度命中专项场景
2. 多 lane 交叉专项场景
3. 高速扫掠命中专项场景
4. 模板实例化专项场景
5. 批量回归脚本和汇总输出

---

## 与验证清单的关系

- [验证清单](15-验证清单.md) 更适合回答“当前有哪些入口、怎么手动和自动验证”
- 这篇矩阵更适合回答“哪些能力已经被覆盖、哪些还缺”

两者应同步维护，但不应互相替代。

---

## 相关文档

- [验证清单](15-验证清单.md)
- [开发路线图](26-开发路线图.md)
- [项目开发方法论](27-项目开发方法论.md)
