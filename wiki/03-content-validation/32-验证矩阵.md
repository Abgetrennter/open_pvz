# 验证矩阵

> 这篇文档把 [验证清单](15-验证清单.md) 从“说明当前有哪些验证入口”进一步推进到“工程上如何持续维护验证覆盖”的矩阵文档。

---

## 文档定位

这篇文档主要回答：

- 当前主干能力分别由哪些场景和命令覆盖
- 哪些验证已经通过，哪些仍缺口明显
- 每个验证项应观察什么结果
- 后续新增能力时，如何把它挂进统一矩阵

这篇文档不负责：

- 重新解释验证脚本的细节
- 代替路线图阶段判断

---

## 状态说明

矩阵中的状态建议统一使用：

- `已通过`
- `部分覆盖`
- `待补充`
- `阻塞`

如果某项验证只在单场景或人工观察下成立，应标为 `部分覆盖`，而不是直接写成 `已通过`。

---

## 当前固定入口

### 1. 默认主场景

- [`scenes/validation/minimal_battle_validation.tscn`](../../scenes/validation/minimal_battle_validation.tscn)

### 2. 主干验证资源

- [`scenes/validation/minimal_battle_validation.tres`](../../scenes/validation/minimal_battle_validation.tres)

### 3. 长距离抛物线专项验证

- [`scenes/validation/parabola_long_range_validation.tres`](../../scenes/validation/parabola_long_range_validation.tres)

### 4. 高度命中专项验证

- [`scenes/validation/height_hit_validation.tres`](../../scenes/validation/height_hit_validation.tres)

### 5. Lane 隔离专项验证

- [`scenes/validation/lane_isolation_validation.tres`](../../scenes/validation/lane_isolation_validation.tres)

### 6. 高速扫掠命中专项验证

- [`scenes/validation/swept_segment_validation.tres`](../../scenes/validation/swept_segment_validation.tres)

### 7. 终点爆炸专项验证

- [`scenes/validation/terminal_explode_validation.tres`](../../scenes/validation/terminal_explode_validation.tres)

### 8. 模板实例化专项验证

- [`scenes/validation/template_instantiation_validation.tres`](../../scenes/validation/template_instantiation_validation.tres)

### 9. 模板工厂专项验证

- [`scenes/validation/template_factory_validation.tres`](../../scenes/validation/template_factory_validation.tres)

### 10. 规则协议负向专项验证

- [`scenes/validation/protocol_guardrail_validation.tres`](../../scenes/validation/protocol_guardrail_validation.tres)

### 11. 空中拦截样例专项验证

- [`scenes/validation/air_interceptor_validation.tres`](../../scenes/validation/air_interceptor_validation.tres)

### 12. 连发射手样例专项验证

- [`scenes/validation/repeater_burst_validation.tres`](../../scenes/validation/repeater_burst_validation.tres)

### 13. 抛投样例专项验证

- [`scenes/validation/lobber_catalog_validation.tres`](../../scenes/validation/lobber_catalog_validation.tres)

### 14. 僵尸梯队样例专项验证

- [`scenes/validation/zombie_roster_attack_validation.tres`](../../scenes/validation/zombie_roster_attack_validation.tres)

### 15. 模板守卫负向专项验证

- [`scenes/validation/template_guardrail_validation.tres`](../../scenes/validation/template_guardrail_validation.tres)

### 16. 自动化脚本

- [`tools/run_validation.ps1`](../../tools/run_validation.ps1)
- [`tools/run_all_validations.ps1`](../../tools/run_all_validations.ps1)
- [`tools/validation_scenarios.json`](../../tools/validation_scenarios.json)

Godot console 路径和本地命令约定见：

- [`agent.md`](../../agent.md)

---

## 当前主干验证矩阵

| 能力域 | 验证项 | 场景 / 资源 | 入口 | 关键观察点 | 当前状态 | 备注 |
|------|------|------|------|------|------|------|
| 启动与资源加载 | 默认主场景能加载 | `minimal_battle_validation.tscn` | headless 启动 | 无脚本解析错误、无场景加载错误 | 已通过 | 作为所有改动后的基础检查 |
| 事件总线 | 结构化事件链可广播 | `minimal_battle_validation.tres` | 默认主场景或指定场景 | `game.tick` 与后续事件链出现 | 已通过 | 当前可观察 `event_id / chain_id / depth` |
| 触发器执行 | 周期触发能驱动效果 | `minimal_battle_validation.tres` | 默认主场景 | `game.tick -> trigger -> effect` | 已通过 | 当前由最小战斗场景覆盖 |
| 规则协议正向一致性 | 第一版触发器 / 效果 / slot / 资源类型白名单能承载当前主干样例 | `minimal_battle_validation.tres` + `terminal_explode_validation.tres` + `template_instantiation_validation.tres` | 默认主场景、指定场景或批量回归 | 正向样例稳定使用白名单项，且事件链与效果链保持一致 | 已通过 | 当前主干样例已稳定落在第一版白名单范围内 |
| 基础效果 | `spawn_projectile / damage / explode` 可执行 | `minimal_battle_validation.tres` | 默认主场景 | 事件链与效果日志一致 | 已通过 | 仍需后续补更多效果类型 |
| 直线投射物 | 线性投射体可生成和命中 | `minimal_battle_validation.tres` | 默认主场景 | `projectile.spawned` 与 `projectile.hit` | 已通过 | 当前主干已覆盖 |
| 追踪投射物 | 追踪投射体可生成和命中 | `minimal_battle_validation.tres` | 默认主场景 | `move_mode = track` 的生成与命中 | 已通过 | 当前主干已覆盖 |
| 抛物线投射物 | 抛射体能在终点命中 | `minimal_battle_validation.tres` | 默认主场景 | `move_mode = parabola` 且终点命中 | 已通过 | 已转为地面平面 + 高度规则 |
| 长距离抛物线 | 远距离抛射体不提前消失且能命中 | `parabola_long_range_validation.tres` | `.\tools\run_validation.ps1` | `projectile.spawned -> projectile.hit -> entity.damaged` | 已通过 | 当前有专项自动化入口 |
| 死亡触发链 | 死亡后续效果可回写 | `minimal_battle_validation.tres` | 默认主场景 | `entity.died` 与爆炸伤害链 | 已通过 | 覆盖最小连锁 |
| 高度规则 | `HeightBand` 对命中真正生效 | `height_hit_validation.tres` | `.\tools\run_validation.ps1` 或批量回归 | `linear_ground` 不命中高空目标，`linear_air` 可命中同一目标 | 已通过 | 当前已由专项场景覆盖，并接入批量回归 |
| 多 lane 交叉 | 轨迹不会被错误 lane 截获 | `lane_isolation_validation.tres` | `.\tools\run_validation.ps1` 或批量回归 | 屏幕投影跨 lane 时，命中仍只落在预期 `target_lane` | 已通过 | 当前已由专项场景覆盖，并接入批量回归 |
| 高速扫掠命中 | `swept_segment` 对高速弹稳定生效 | `swept_segment_validation.tres` | `.\tools\run_validation.ps1` 或批量回归 | 单帧跨过目标时仍出现 `projectile.hit`，而不是 `projectile.expired` | 已通过 | 当前以高速直线投射体专项场景覆盖 |
| 终点爆炸 | 终点命中可稳定接出 `explode` 链 | `terminal_explode_validation.tres` | `.\tools\run_validation.ps1` 或批量回归 | `projectile.hit` 以终点原因发生，随后至少 2 个目标收到 `explode` 伤害 | 已通过 | 当前以抛物线终点爆炸专项场景覆盖 |
| 模板实例化 | 模板驱动实体生成 | `template_instantiation_validation.tres` | `.\tools\run_validation.ps1` 或批量回归 | `BattleSpawnEntry` 可引用 `EntityTemplate`，模板 id、投射物模板和飞行 profile 会进入运行时事件链 | 已通过 | 当前最小模板实例化链已稳定落地，并已开始区分实体模板与投射物模板 |
| 模板工厂 | 模板定义的运行时 trigger 由工厂生成 | `template_factory_validation.tres` | `.\tools\run_validation.ps1` 或批量回归 | 模板定义的攻击、反击和死亡行为可由 `TriggerBinding` 产出 trigger 实例 | 已通过 | `BattleManager` 已收敛为消费工厂结果，而不是再做内容级行为装配 |
| 模板覆盖优先级 | 场景条目可按模板 id 实例化，并用局部覆盖替换模板默认值 | `spawn_override_priority_validation.tres` | `.\tools\run_validation.ps1` 或批量回归 | `entity_template_id` 可通过注册入口解析，且 `spawn_overrides / projectile_*_override` 会优先于模板和绑定默认投射配置 | 已通过 | 当前已验证场景局部覆盖优先于模板默认投射入口，便于后续继续收紧 `BattleSpawnEntry` 职责 |
| 空中拦截样例 | 空中追踪投射样例可命中低空和中空目标 | `air_interceptor_validation.tres` | `.\tools\run_validation.ps1` 或批量回归 | `plant_air_interceptor` 产出 `track_air` 投射体，并命中 `zombie_air_scout` 与中空目标 | 已通过 | 当前覆盖了 `track_air`、`air_unit_low` 和 `air_unit_medium` 样例 |
| 连发射手样例 | 连发射手样例可稳定生成多枚同类投射体并在双 lane 命中 | `repeater_burst_validation.tres` | `.\tools\run_validation.ps1` 或批量回归 | `plant_repeater_burst` 至少生成 4 枚 `pea_burst`，并分别命中 `zombie_basic_walker` 与 `zombie_brisk_runner` | 已通过 | 当前覆盖了重复直线射击样例和基础/快跑僵尸样例 |
| 抛投样例 | 抛投样例可分别完成直接命中与终点爆炸 | `lobber_catalog_validation.tres` | `.\tools\run_validation.ps1` 或批量回归 | `plant_cabbage_lobber` 命中 `zombie_bucket_tank`，`plant_melon_lobber` 通过 `melon_blast` 对簇拥目标造成爆炸伤害 | 已通过 | 当前覆盖了 `parabola_cabbage_arc`、`parabola_terminal_blast`、`cabbage_arc`、`melon_blast` 样例 |
| 僵尸梯队样例 | 新僵尸梯队样例可通过统一 bite 运行时攻击障碍单位 | `zombie_roster_attack_validation.tres` | `.\tools\run_validation.ps1` 或批量回归 | `zombie_basic_walker`、`zombie_brisk_runner`、`zombie_bucket_tank`、`zombie_boss_heavy` 都能对 `plant_wall_barrier` 产生 `bite` 伤害事件 | 已通过 | 当前覆盖了基础、快跑、高血和重型僵尸样例，以及防御植物样例 |
| 规则协议守卫 | 第一版触发器 / 效果 / slot / 资源类型白名单会阻断非法配置 | `protocol_guardrail_validation.tres` | `.\tools\run_validation.ps1` 或批量回归 | 非法子效果、错误资源类型、不支持参数、错误条件类型、不支持的 `target_mode`、未知效果 id、错误覆盖结构都会进入 `protocol.issue`，且不会继续生成投射物或伤害事件 | 已通过 | 当前已覆盖一批贴近主链的非法组合，并开始把第三阶段第一轮效果目标语义也接入守卫矩阵 |
| 模板与覆盖守卫 | 模板资源缺陷、错误 override 资源和不一致组合会在运行前被阻断 | `template_guardrail_validation.tres` | `.\tools\run_validation.ps1` 或批量回归 | 缺失模板 id、非法 trigger binding、不支持的 `behavior_key`、`event_name` 漂移、运行时非法键、无法解析的 `entity_template_id`、错误 `projectile_template_override`、`movement_mode` 与 `flight_profile` 不一致都会进入 `protocol.issue`，且不会继续生成投射物或伤害事件 | 已通过 | 当前把第二阶段新增模板入口、第三阶段第一轮 TriggerBinding 入口约束和覆盖字段的常见错误接入了负向守卫矩阵 |
| 批量回归 | 多场景可一次性执行 | `validation_scenarios.json` | `.\tools\run_all_validations.ps1` | 生成 `summary.json / summary.txt`，并统一汇总 pass/fail | 已通过 | 当前已接入最小场景、长距离抛射、高度命中、lane 隔离、swept 命中、终点爆炸、模板实例化、模板工厂和协议守卫专项 |
| 回归记录 | 批量回归状态可持续沉淀 | `artifacts/validation/regression_status.json` | `.\tools\run_all_validations.ps1` | 自动更新 latest、history 和 per-scenario 状态账本 | 已通过 | 当前会记录累计 pass/fail、连续通过次数和最近一次运行位置 |

---

## 第四阶段玩法层专项预留

| 能力域 | 验证项 | 场景 / 资源 | 入口 | 关键观察点 | 当前状态 | 备注 |
|------|------|------|------|------|------|------|
| 阳光与资源 | 资源产出、收集与消耗闭环 | `sun_resource_validation.tres` | `.\tools\run_validation.ps1` 或批量回归 | `sun.spawned -> sun.collected -> resource.changed`；资源不足时出现 `resource.spend_failed`；资源足够时出现成功扣费对应的 `resource.changed` | 已通过 | 已接入 [第四阶段-阳光与资源最小概念草案](../../plans/第四阶段-阳光与资源最小概念草案.md) 与 `tools/validation_scenarios.json` |
| 棋盘、占位与放置 | 合法格占位、角色分层、槽位标签与模板静态约束 | `board_placement_validation.tres` + `board_slot_tag_validation.tres` + `roof_slot_validation.tres` + `air_slot_validation.tres` + `cover_blocker_validation.tres` | `.\tools\run_validation.ps1` 或批量回归 | 合法请求进入正式棋盘层并触发 `placement.accepted`；已占用或非法格触发 `placement.rejected`；槽位 `slot_type + effective_tags` 会拦截标签不匹配请求；模板 `placement_role + allowed_slot_types + required_placement_tags + required_present_roles + required_empty_roles` 会参与棋盘层合法性判定；`surface + primary`、`primary + cover` 可在同一 slot 分层共存；`blocker` 会阻断其它角色；`roof` 需要 surface 容器承托；`air_only` 只接受匹配静态约束的模板；被拒绝请求不会生成额外实体。当前最小 formal slot type 白名单为 `ground / water / roof / air_only`，其中“container”通过 `surface` 角色表达，而不是作为独立基础 slot type。 | 已通过 | 已接入 [第四阶段-棋盘占位放置最小概念草案](../../plans/第四阶段-棋盘占位放置最小概念草案.md) 与 `tools/validation_scenarios.json` |
| 卡片、费用、冷却与手牌 | 出牌闭环与失败语义 | `card_flow_validation.tres` | `.\tools\run_validation.ps1` 或批量回归 | `card.selected -> card.play_requested -> resource.changed(reason=card_play) -> placement.accepted -> entity.spawned -> card.cooldown_started`；失败时 `card.play_rejected`，资源不足同时伴随 `resource.spend_failed` | 已通过 | 已接入 [第四阶段-卡片手牌费用最小概念草案](../../plans/第四阶段-卡片手牌费用最小概念草案.md) 与 `tools/validation_scenarios.json` |
| 波次、战局阶段与胜负条件 | 波次推进、阶段切换、胜负判定与守卫 | `wave_flow_validation.tres` + `wave_guardrail_validation.tres` | `.\tools\run_validation.ps1` 或批量回归 | `battle.phase_changed` 会从 `preparing` 进入 `running` 并在结束时进入 `victory / defeat`；`wave.started / wave.completed` 与波次生成链一致；波次定义的敌方条目通过共享 spawn 主链进入战局；无效波次定义会进入 `protocol.issue` 且不会继续生成波次敌人 | 已通过 | 已接入 [第四阶段-波次战局阶段胜负条件最小概念草案](../../plans/第四阶段-波次战局阶段胜负条件最小概念草案.md) 与 `tools/validation_scenarios.json` |

接入顺序建议：

1. `sun_resource_validation`
2. `board_placement_validation`
3. `card_flow_validation`
4. `wave_flow_validation`
5. `board_slot_tag_validation`
6. `roof_slot_validation`
7. `air_slot_validation`
8. `wave_flow_validation`
9. `wave_guardrail_validation`

当前状态说明：

- 本区块用于为第四阶段第一批玩法层专项预留矩阵位置
- 当前不要求这些场景已经写入 `tools/validation_scenarios.json`
- 等对应实现进入主干后，再按上面的顺序接入批量回归

---

## 当前推荐命令

### 1. 基础 headless 启动

```powershell
& '<GodotConsole>' --headless --path '<project_root>' --quit-after 3
```

### 2. 跑当前默认自动化验证

```powershell
& '.\tools\run_validation.ps1'
```

### 3. 指定验证资源

```powershell
& '.\tools\run_validation.ps1' -Scenario 'res://scenes/validation/minimal_battle_validation.tres'
```

### 4. 打开运行时快照日志

```powershell
& '<GodotConsole>' --headless --path '<project_root>' --quit-after 240 -- --runtime-snapshot-log --runtime-snapshot-interval=30
```

### 5. 批量执行当前验证集

```powershell
& '.\tools\run_all_validations.ps1'
```

---

## 新增验证项时的维护规则

以后每增加一个新的主干能力，建议至少回答下面 6 个问题。

1. 它属于哪个能力域。
2. 它对应哪个最小验证场景。
3. 它是否有自动化入口。
4. 它的关键日志看什么。
5. 当前状态是 `已通过 / 部分覆盖 / 待补充 / 阻塞` 哪一种。
6. 如果还没通过，下一步要补什么。

如果一项能力已经进入主干，但在这张矩阵里没有位置，说明验证体系还没有真正接住它。

---

## 当前最应该补的矩阵空洞

按优先级看，当前最值得补的是：

1. 按系统域继续扩展 `validation_scenarios.json`
2. 战斗玩法层专项验证预留位
3. 更多非法组合和协议边界专项
4. 模板批量内容样例扩展
5. 回归趋势可视化

---

## 与验证清单的关系

- [验证清单](15-验证清单.md) 更适合回答“当前有哪些入口、怎么手动和自动验证”
- 这篇矩阵更适合回答“哪些能力已经被覆盖、哪些还缺”

两者应同步维护，但不应互相替代。

---

## 相关文档

- [验证清单](15-验证清单.md)
- [开发路线图](../04-roadmap-reference/26-开发路线图.md)
- [项目开发方法论](../05-governance/27-项目开发方法论.md)
